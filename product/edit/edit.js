// import what you need


// reload window when changing product
window.addEventListener('hashchange', ()=> {
    window.location.reload(true) // ca actualise la page berk
})


// get id - check how we did in product.js


// start
document.addEventListener('DOMContentLoaded', ()=>{
    // fetch product, then create form, select the save button (check how we did it in product.js)
    // attach it to the click handler (do exactly like in add.js), and add the section to DOM 

})

// load the form with old informations of product
const Form = () => {
  // TO DO

}


// when click on save button. Put all the inputs value in an object, and send the Patch request !
// if an error occured, print it to console
// after all of that, go to home page (like you did in add.js)
const handleSave = () => {
    alert('save button clicked!')

    // TO DO
}